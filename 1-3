//請寫出一個函數可以檢查數值 n 是否為質數
//並寫一個主程式可以列出 1 到 1000 之間的所有質數。


//確認是否為質數
function isPrime (n) {
    for (i=2;i<n;i++){
        if (n%i === 0)
        return false;
    }
    return true;
}


for (j=2;j<=1000;j++){
    if (isPrime(j))               //if (true) {執行}
    console.log(i);
}

