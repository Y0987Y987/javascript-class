//請寫出一個可以將兩向量相乘的函數 mul(a,b)，並用 mul([1,2,3,4], [4,3,2,1]) 進行測試。

var a =[1,2,3,4];
var b =[4,3,2,1];

function mul(x,y){
	var c = [];
	for (var i=0;i<a.length;i++)   //The length property性質 sets or returns the number of elements in an array.
		c[i]=a[i]*b[i];
		return c;
}

console.log(mul(a,b));
