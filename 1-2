var a =[1,2,3,4];
var b =[4,3,2,1];

function mul(x,y){
	var c = [];
	for (var i=0;i<a.length;i++)   //The length property性質 sets or returns the number of elements in an array.
		c[i]=a[i]*b[i];
		return c;
}

d=mul(a,b);

console.log(d);
